{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt -r requirements-vnpy.txt && python -c \"from pathlib import Path; p=Path('config/kk/symbols.txt'); assert p.is_file(), 'missing config/kk/symbols.txt'; print('kk symbols ok', len(p.read_text().splitlines()))\""
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
